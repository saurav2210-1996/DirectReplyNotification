package com.codility.video_compress

class Sample(offset: Long, size: Long) {
    var offset: Long = 0
    var size: Long = 0

    init {
        this.offset = offset
        this.size = size
    }
}
